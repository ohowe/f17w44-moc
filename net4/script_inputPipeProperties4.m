% script_inputPipeProperties

i = 1;
    j=1;
    pipe(i,j).tag = 'L1';
    pipe(i,j).c  = 1000; % wave speed
    pipe(i,j).D  = 0.063;  % diameter
    pipe(i,j).f  = 0.02; % friction factor
    pipe(i,j).Nx = 500; % number of reaches
    j=2;
    pipe(i,j).tag = 'L1A';
    pipe(i,j).c  = 1000; % wave speed
    pipe(i,j).D  = 0.063;  % diameter
    pipe(i,j).f  = 0.02; % friction factor
    pipe(i,j).Nx = 200; % number of reaches 
i = 2;
    j=1;
    pipe(i,j).tag = 'L2';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.063;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 120;
    j=2;
    pipe(i,j).tag = 'L2A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.063;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 120;
i = 3;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.05;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 200;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.008;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 10;
i = 4;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.04;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 40;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.04;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 40;
i = 5;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.01;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 30;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.005;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 10;
i = 6;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 60;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 60;
i = 7;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 20;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.008;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 10;
i = 8;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 80;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 20;
i = 9;
    j=1;
    pipe(i,j).tag = 'L1';
    pipe(i,j).c  = 1000; 
    pipe(i,j).D  = 0.015; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 40; 
    j=2;
    pipe(i,j).tag = 'L1A';
    pipe(i,j).c  = 1000; 
    pipe(i,j).D  = 0.008;  
    pipe(i,j).f  = 0.02; 
    pipe(i,j).Nx = 10;  
i = 10;
    j=1;
    pipe(i,j).tag = 'L1';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.05; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 50; 
    j=2;
    pipe(i,j).tag = 'L1A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.05; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 50;  
i = 11;
    j=1;
    pipe(i,j).tag = 'L1';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.02; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 30; 
    j=2;
    pipe(i,j).tag = 'L1A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.005; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 10;  
i = 12;
    j=1;
    pipe(i,j).tag = 'L1';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.04; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 16; 
    j=2;
    pipe(i,j).tag = 'L1A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.04; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 16;  
i = 13;
    j=1;
    pipe(i,j).tag = 'L1';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 6; 
    j=2;
    pipe(i,j).tag = 'L1A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 6;  
i = 14;
    j=1;
    pipe(i,j).tag = 'L1';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.02; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 20; 
    j=2;
    pipe(i,j).tag = 'L1A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.005; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 10;  
i = 15;
    j=1;
    pipe(i,j).tag = 'L1';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 30; 
    j=2;
    pipe(i,j).tag = 'L1A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.025; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 10; 
i = 16;
    j=1;
    pipe(i,j).tag = 'L2';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.02;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 30;
    j=2;
    pipe(i,j).tag = 'L2A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.01;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 10;
i = 17;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 80;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 50;
i = 18;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 20;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.032;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 10;
i = 19;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.02;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 20;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.005;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 10;
i = 20;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.02;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 20;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.005;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 10;
i = 21;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.025;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 16;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.025;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 16;
i = 22;
    j=1;
    pipe(i,j).tag = 'L3';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.02;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 30;
    j=2;
    pipe(i,j).tag = 'L3A';
    pipe(i,j).c  = 1000;
    pipe(i,j).D  = 0.008;
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 10;
i = 23;
    j=1;
    pipe(i,j).tag = 'L1';
    pipe(i,j).c  = 1000; 
    pipe(i,j).D  = 0.025; 
    pipe(i,j).f  = 0.02;
    pipe(i,j).Nx = 70; 
    j=2;
    pipe(i,j).tag = 'L1A';
    pipe(i,j).c  = 1000; 
    pipe(i,j).D  = 0.01;  
    pipe(i,j).f  = 0.02; 
    pipe(i,j).Nx = 10;  